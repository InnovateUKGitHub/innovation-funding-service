// Deployment behaviour for differing versions has not been tested yet
// and so version numbers should always equal project.version
// DO NOT HAVE OUT OF SYNC VERSION NUMBERS - THIS WILL BE IMPLEMENTED AS PART OF IFS-334
ext.dataServiceVersion = project.properties['data-service.version'] ?: project.version
ext.financeDataServiceVersion=project.properties['finance-data-service.version'] ?:project.version
ext.alertServiceVersion = project.properties['alerts-service.version'] ?: project.version
ext.surveyDataServiceVersion=project.properties['survey-data-service.version'] ?:project.version

ext.applicationServiceVersion = project.properties['application-service.version'] ?: project.version
ext.surveyServiceVersion = project.properties['survey-service.version'] ?: project.version
ext.frontDoorServiceVersion = project.properties['front-door-service.version'] ?: project.version
ext.assessmentServiceVersion = project.properties['assessment-service.version'] ?: project.version
ext.competitionMgtServiceVersion = project.properties['competition-mgt-service.version'] ?: project.version
ext.projectSetupMgtServiceVersion = project.properties['project-setup-mgt-service.version'] ?: project.version
ext.projectSetupServiceVersion = project.properties['project-setup-service.version'] ?: project.version
ext.registrationServiceVersion = project.properties['registration-service.version'] ?: project.version
ext.prototypesServiceVersion = project.properties['prototypes-service.version'] ?: project.version

ext.spVersion = project.properties['sp.version'] ?: project.version
ext.idpVersion = project.properties['idp.version'] ?: project.version
ext.ldapVersion = project.properties['ldap.version'] ?: project.version

ext.mailSinkVersion = project.properties['mail-sink.version'] ?: project.version
ext.mysqlClientVersion = project.properties['mysql-client.version'] ?: project.version

ext.silStubVersion = project.properties['sil-stub.version'] ?: project.version

ext['hibernate.version'] = '5.2.7.Final'
ext['thymeleaf-extras-springsecurity4.version'] = '3.0.2.RELEASE'
ext['thymeleaf-extras-java8time.version'] = '3.0.0.RELEASE'