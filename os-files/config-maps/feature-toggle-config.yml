kind: ConfigMap
apiVersion: v1
metadata:
  name: feature-toggle-config
data:
  IFS_PROCUREMENT_MILESTONES_ENABLED: "${IFS_PROCUREMENT_MILESTONES_ENABLED}"
  IFS_ALWAYS_OPEN_COMPETITION_ENABLED: "${IFS_ALWAYS_OPEN_COMPETITION_ENABLED}"