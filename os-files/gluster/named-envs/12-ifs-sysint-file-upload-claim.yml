apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sysint-file-upload-claim
spec:
  accessModes:
  - ReadWriteMany
  resources:
     requests:
       # storage: 20G
       storage: 500M
  selector:
    matchLabels:
      # ifssysint: "ifssysint"
      dev: "dev"
