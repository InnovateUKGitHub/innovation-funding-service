commonAnnotations:
  deployedWith: kustomize
  environment: perf

bases:
  - ../../base

patchesStrategicMerge:
  - auth/sp/sp-deployment.yml
  - data-layer/data-service/data-service-deployment.yml
  - web-layer/registration-service/registration-service-deployment.yml