apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-setup-mgt-svc
  annotations:
    monitor: monitor
    pullSecrets: pullSecrets
spec:
  replicas: 2
  selector:
    matchLabels:
      app: project-setup-mgt-svc
  template:
    metadata:
      labels:
        app: project-setup-mgt-svc