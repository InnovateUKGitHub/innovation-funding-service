apiVersion: apps/v1
kind: Deployment
metadata:
  name: competition-mgt-svc
  annotations:
    monitor: monitor
    pullSecrets: pullSecrets
spec:
  replicas: 4
  selector:
    matchLabels:
      app: competition-mgt-svc
  template:
    metadata:
      labels:
        app: competition-mgt-svc