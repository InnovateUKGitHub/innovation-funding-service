apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-rolling-update-patch
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1 #So Flyway runs on one thread at a time
      maxUnavailable: 0