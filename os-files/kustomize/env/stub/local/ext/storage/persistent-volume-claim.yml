apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: file-upload-claim
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200M
  storageClassName: manual