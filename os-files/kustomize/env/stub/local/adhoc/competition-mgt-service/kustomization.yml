commonAnnotations:
  deployedWith: kustomize
  environment: local-dev

bases:
  - ../../local-base

components:

  - ../../../../../components/web-layer/competition-mgt-service
  - ../../../../../patches/
  - ../../patches/
