apiVersion: apps/v1
kind: Deployment
metadata:
  name: survey-svc
spec:
  template:
    spec:
      containers:
        - name: spring-boot-service-container
          startupProbe:
            httpGet:
              path: /survey/health
              port: 8080