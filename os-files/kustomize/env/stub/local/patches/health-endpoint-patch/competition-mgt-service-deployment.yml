apiVersion: apps/v1
kind: Deployment
metadata:
  name: competition-mgt-svc
spec:
  template:
    spec:
      containers:
        - name: spring-boot-service-container
          startupProbe:
            httpGet:
              path: /management/health
              port: 8080