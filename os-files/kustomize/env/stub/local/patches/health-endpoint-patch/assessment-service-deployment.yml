apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-svc
spec:
  template:
    spec:
      containers:
        - name: spring-boot-service-container
          startupProbe:
            httpGet:
              path: /monitoring/assessment
              port: 8080