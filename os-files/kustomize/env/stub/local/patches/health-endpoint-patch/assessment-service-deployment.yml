apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-svc
spec:
  template:
    spec:
      containers:
        - name: spring-boot-service-container
          startupProbe:
            httpGet:
              path: /assessment/monitoring/health
              port: 8080