apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-setup-svc
spec:
  template:
    spec:
      containers:
        - name: spring-boot-service-container
          livenessProbe:
            httpGet:
              path: /project-setup/monitoring/health
              port: 8080
          readinessProbe:
            httpGet:
              path: /project-setup/monitoring/health
              port: 8080
          startupProbe:
            httpGet:
              path: /project-setup/monitoring/health
              port: 8080