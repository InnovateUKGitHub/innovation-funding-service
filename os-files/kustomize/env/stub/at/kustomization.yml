apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonAnnotations:
  deployedWith: kustomize
  environment: at

components:
  - ../../../components/sba
  - ../../../components/robot
  - ../../../patches/pull-secrets-patch