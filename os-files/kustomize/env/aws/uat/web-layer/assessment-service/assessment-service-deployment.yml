apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-svc
  annotations:
    monitor: monitor
    pullSecrets: pullSecrets
spec:
  replicas: 2
  selector:
    matchLabels:
      app: assessment-svc
  template:
    metadata:
      labels:
        app: assessment-svc