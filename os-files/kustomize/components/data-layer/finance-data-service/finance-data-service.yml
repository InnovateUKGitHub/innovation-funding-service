apiVersion: v1
kind: Service
metadata:
  name: finance-data-service
  labels:
    app: finance-data-service
  annotations:
    javaDebug: javaDebug
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      targetPort: 8080
  selector:
    app: finance-data-service
