apiVersion: v1
kind: Service
metadata:
  name: survey-data-svc
  labels:
    app: survey-data-svc
  annotations:
    javaDebug: javaDebug
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      targetPort: 8080
  selector:
    app: survey-data-svc
