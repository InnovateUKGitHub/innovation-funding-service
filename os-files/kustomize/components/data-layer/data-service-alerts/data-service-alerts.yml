apiVersion: v1
kind: Service
metadata:
  name: data-service-alerts
  labels:
    run: data-service-alerts
  annotations:
    javaDebug: javaDebug
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      targetPort: 8080
  selector:
    app: data-service-alerts
