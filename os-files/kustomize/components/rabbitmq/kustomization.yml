apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  # This is a minimal (very) non-production rabbit for local and non load use
  # Single instance, no persistence with minimum specs allowed
  - rabbitmq.yml
  - rabbitmq-deployment.yml