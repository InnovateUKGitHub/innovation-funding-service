apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secrets
type: Opaque
stringData:
  # Ifs message-starter consume these
  IFS_RABBIT_USERNAME: "ifs"
  IFS_RABBIT_PASSWORD: "ifs"
  # The rabbit containers consume these
  RABBITMQ_DEFAULT_USER: "ifs"
  RABBITMQ_DEFAULT_PASS: "ifs"