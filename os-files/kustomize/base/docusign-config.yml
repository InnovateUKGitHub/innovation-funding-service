kind: ConfigMap
apiVersion: v1
metadata:
  name: docusign-config
data:
  IFS_DOCUSIGN_API_ACCOUNT: "${IFS_DOCUSIGN_API_ACCOUNT}"
  IFS_DOCUSIGN_API_BASE: "${IFS_DOCUSIGN_API_BASE}"
  IFS_DOCUSIGN_AUTH_BASE: "${IFS_DOCUSIGN_AUTH_BASE}"
  IFS_DOCUSIGN_AUTH_USER: "${IFS_DOCUSIGN_AUTH_USER}"
  IFS_DOCUSIGN_AUTH_CLIENT: "${IFS_DOCUSIGN_AUTH_CLIENT}"
  IFS_DOCUSIGN_AUTH_KEY: "${IFS_DOCUSIGN_AUTH_KEY}"