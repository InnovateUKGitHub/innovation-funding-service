## Redis Config -  defaults not set via env variables (k8s config-map)
## AT and custom
spring:
  cache:
    type: REDIS
  redis:
    clientType: LETTUCE
    timeout: 1s
    enable-statistics: true
  data:
    redis:
      repositories:
        enabled: false
