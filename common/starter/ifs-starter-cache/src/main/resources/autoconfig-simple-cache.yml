## Simple Cache Config - development only
spring:
  cache:
    type: SIMPLE
  data:
    redis:
      repositories:
        enabled: false
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
