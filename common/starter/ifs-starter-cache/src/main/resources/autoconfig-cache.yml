spring:
  cache:
    type: simple
  redis:
    # Set to jedis to disable lettuce
    client-type: jedis
  data:
    redis:
      repositories:
        enabled: false
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration