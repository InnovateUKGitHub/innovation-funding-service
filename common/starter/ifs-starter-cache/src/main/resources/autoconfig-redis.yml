## Redis Config - no defaults not set via env variables (k8s config-map)
spring:
  redis:
    lettuce:
      pool:
        max-active: 8
    timeout: 1s
    cluster:
      maxRedirects: 3
  data:
    redis:
      repositories:
        enabled: false