server:
  port: 7443
spring:
  cloud:
    kubernetes:
      discovery:
        include-not-ready-addresses: true
        all-namespaces: true
  boot:
    admin:
      discovery:
        converter:
          management-context-path: /monitoring

ifs:
  sba:
    username: ${IFS_SBA_USERNAME}
    password: ${IFS_SBA_PASSWORD}