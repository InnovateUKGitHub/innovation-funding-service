# Configure your ports here to avoid any port conflicts with existing processes
# that are running on your localhost.

# The variable on the left represents the port that Docker will use internally
# whilst the value your provide on the right is the localhost mapped port.

WEB_PORT_12345=12345
WEB_PORT_8081=8081
WEB_JAVA_OPTS=-Xms256m -Xmx512m

DATA_PORT_12345=12346
DATA_PORT_8081=8091
DATA_JAVA_OPTS=-Xms256m -Xmx512m