version: '2'
services:

  shib:
    image: g2g3/ifs-local-dev
    cap_add:
      - SYS_PTRACE
    ports:
      - "443:443"
      - "389:389"
    links:
      - web
      - data
    networks:
      front-tier:
        aliases:
         - iuk-auth-localdev
         - ifs-local-dev

  web:
    build:
      context: .
      dockerfile: web-dockerfile
    ports:
      - "8080:8080"
      - "12345:12345"
      - "8081:8081"
    links:
      - data
    networks:
      front-tier:
        aliases:
          - ifs-web-service
          - ifs-application-host

  data:
    build:
      context: .
      dockerfile: data-dockerfile
    volumes:
      - ./robot-tests/upload_files/:/tmp/ifs-local
    ports:
      - "8090:8080"
      - "12346:12345"
      - "8091:8081"
    depends_on:
      - mysql
    networks:
      front-tier:
        aliases:
          - data-service
          - ifs-data-service
      back-tier:
        aliases:
          - data-service
          - ifs-data-service

  mysql:
    image: mysql:5.5.49
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: ifs
    restart: on-failure
    networks:
      back-tier:
        aliases:
         - database
         - ifs-database

  smtp:
    image: camptocamp/smtp-sink
    expose:
      - 25
    networks:
      back-tier:
        aliases:
          - smtp

  imap:
    image: camptocamp/courier-imap
    volumes_from:
      - smtp:rw
    expose:
      - 143
    networks:
      back-tier:
        aliases:
          - imap

  webmail:
    image: camptocamp/sqwebmail
    ports:
      - "1234:80"
    depends_on:
      - smtp
    volumes_from:
      - smtp:rw
    networks:
      back-tier:
        aliases:
          - webmail


  imap-ssl:
    image: purplebooth/nginx-ssl-terminator
    ports:
      - "9876:443"
    environment:
      UPSTREAM: imap:143
    depends_on:
      - imap
    networks:
      back-tier:
        aliases:
          - imap-ssl
      front-tier:
        aliases:
          - imap

networks:
  front-tier:
    driver: bridge
  back-tier:
    driver: bridge
