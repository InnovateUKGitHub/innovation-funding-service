apply from: rootProject.file("gradle-support/openshift-build.gradle")
buildDocker.dependsOn ":docker:oracle-jre:pullAndTagImage"

[stop, removeContainer, createContainer, deploy, wait].each { task ->
    task.enabled = false
}
