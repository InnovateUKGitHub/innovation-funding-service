subprojects {
    apply from: rootProject.file("openshift-build.gradle")
}


    allprojects {

        buildscript {

            repositories {
                mavenCentral()
            }
        }

    }

