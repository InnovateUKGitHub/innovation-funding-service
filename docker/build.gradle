subprojects {
    apply from: rootProject.file("openshift-build.gradle")
    buildDocker.tag = "innovateuk/${project.name}"
}


    allprojects {

        buildscript {

            repositories {
                mavenCentral()
            }
        }

    }

