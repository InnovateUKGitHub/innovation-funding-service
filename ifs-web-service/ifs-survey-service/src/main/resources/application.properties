spring.application.name=Survey Service
server.servlet.context-path=/survey

ifs.survey.data.service.baseURL=http://survey-data-service:8080

spring.boot.admin.client.service-url=http://survey-svc:8080/survey

# The validation fails without this
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false