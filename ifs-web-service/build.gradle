if (project.hasProperty("profile")) {
    apply from: "${project.profile}-build.gradle"
} else {
    apply from: "docker-build.gradle"
}

task cleanDeployServices{
	println "interface for clean deploy services"
}

task deployToTomcat() {
    println "Interface for deployToTomcat task"
}
