package org.innovateuk.ifs.thread.viewmodel;

/**
 * The possible states of a query or note message thread.
 */
public enum ThreadState {
    LAST_POST_BY_INTERNAL_USER,
    LAST_POST_BY_EXTERNAL_USER,
    CLOSED;
}
