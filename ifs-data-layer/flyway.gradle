apply plugin: org.flywaydb.gradle.FlywayPlugin

buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'org.flywaydb:flyway-gradle-plugin:5.1.4'
        classpath 'org.flywaydb:flyway-core:5.1.4'
    }
}

dependencies {
    compile "org.flywaydb:flyway-core:5.1.4"
}
